@model IEnumerable<ATL_WebUI.Models.SQL.Container>

@{
    ViewData["Title"] = "Index";
    Layout = "~/Views/Shared/_LayoutAdmin.cshtml";
}

<hr />
<h4 class="m-0 font-weight-bold text-primary">Container List</h4><hr /><hr />
<p>@Html.ActionLink("Create New", "Create", "Containers", null, new { @class = "btn btn-primary" })</p>
<div class="col-md-12 mb-12">
    <div class="card border-left-primary shadow h-100 py-2">
        <div class="card-body">
            <div class="col-md-12">
                <table class="table table-light">
                    <thead>
                        <tr>
                            <th>
                                @Html.DisplayNameFor(model => model.Name)
                            </th>
                            <th></th>
                        </tr>
                    </thead>
                    <tbody>
                        @foreach (var item in Model)
                        {
                            <tr>
                                <td>
                                    @Html.DisplayFor(modelItem => item.Name)
                                </td>
                                <td>
                                    <a asp-action="Delete" asp-route-id="@item.Unit_Id">Delete</a>
                                </td>
                            </tr>
                        }
                    </tbody>
                </table>
            </div>
        </div>
    </div>
</div>